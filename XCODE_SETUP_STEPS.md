# خطوات ربط Xcode مع GitHub - دليل تفصيلي

## 📍 الموقع الحالي:
- **مشروع Xcode**: `/Users/MBA/Desktop/athkar/`
- **ملفات JSON الجديدة**: `/Users/MBA/Desktop/untitled folder 2/athkar/AthkarJSON/`

---

## ✅ الخطوة 1: تحديث ContentLoader.swift

الكود الحالي يبحث عن الملفات بالأسماء القديمة (مثل `01-Morning Remembrances.json`).
يجب تحديثه ليتوافق مع البنية الجديدة.

### أين تجد الملف:
1. في Xcode، في الـ Navigator (اليسار)
2. ابحث عن: `athkar` → `Data` → `ContentLoader.swift`
3. اضغط عليه لفتحه

### ما يجب تغييره:
الكود يبحث عن الملفات من GitHub مباشرة. يجب تحديث المسارات لتتوافق مع البنية الجديدة.

---

## ✅ الخطوة 2: تحديث مسارات الملفات في ContentLoader.swift

### الملفات القديمة (السطر 29-45):
```swift
("01-Morning Remembrances.json", "morning", .morning),
("02-Evening Remembrances.json", "evening", .evening),
```

### الملفات الجديدة (يجب تحديثها):
```swift
("القائمه الرئيسيه/اذكار الصباح/Morning Remembrances.json", "morning", .morning),
("القائمه الرئيسيه/اذكار المساء/Evening Remembrances.json", "evening", .evening),
```

---

## 🎯 الخطوات في Xcode:

### 1. افتح ContentLoader.swift:
   - في Navigator (اليسار)
   - `athkar` → `Data` → `ContentLoader.swift`
   - اضغط عليه

### 2. ابحث عن `mainMenuFiles` (حوالي السطر 29)

### 3. استبدل أسماء الملفات بالمسارات الجديدة

### 4. احفظ الملف (Cmd+S)

### 5. اختبر التطبيق

---

## 📝 ملاحظات مهمة:

- ✅ الملفات موجودة على GitHub في `AthkarJSON/`
- ✅ الأرقام محذوفة من أسماء الملفات
- ✅ الملفات منظمة في مجلدات عربية
- ⚠️ يجب تحديث `baseURL` في السطر 8 ليشمل `AthkarJSON/`

---

## 🔧 التحديث المطلوب:

### السطر 8 (baseURL):
```swift
private let baseURL = "https://raw.githubusercontent.com/MBA-9/athkar/main/AthkarJSON"
```

### السطر 112 (URL encoding):
يجب أن يدعم المسارات العربية والمجلدات.

---

## 💡 هل تريد مني تحديث الملف تلقائياً؟

يمكنني تحديث `ContentLoader.swift` ليتوافق مع البنية الجديدة.
أخبرني إذا كنت تريد ذلك!

